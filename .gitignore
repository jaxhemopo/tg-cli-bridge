# Go
# Anchored to repo root so it ignores the built binary, NOT the cmd/tg-cli-bridge/ source dir
/tg-cli-bridge
*.test
*.out
coverage.txt
vendor/

# Editor / OS
.DS_Store
.idea/
.vscode/
*.swp

# Local state
*.log
*.bak.*

# User config — examples are tracked, real config never is
config.toml
!examples/config.toml*
